import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'pt.vendus.posHost',
    appName: 'posHost',
    webDir: '../../dist/apps/pos-host',
    bundledWebRuntime: false,
};

export default config;
