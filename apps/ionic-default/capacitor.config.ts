import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicDefault',
  webDir: '../../dist/apps/ionic-default',
  bundledWebRuntime: false,
};

export default config;
